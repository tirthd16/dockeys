todo: dockeys

- add #dd (delete multiple lines)

- add #yy (yank multiple lines)

- add <<, >> to tab / shift tab

- fix ce, de (harder than looks as Google Docs cut by default removes extra space?)

- add deployable relative line numbers (g-line jumping) & marking

- add F{char} - line search?  if can "see" line chars, then can add `{c/d/y}a{ ( / { / " / ' < / t }` and `{c/d/y}i{ / " / ' < / t }`
